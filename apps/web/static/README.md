# apps/web/static

Static assets (CSS, client-side JS, images) served directly by the web app.

## Status

Placeholder. Empty until the frontend scaffold exists. No frontend build
step is planned — see the root `README.md` — so assets here are served as
authored.
