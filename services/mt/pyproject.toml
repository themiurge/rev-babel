[project]
name = "rev-babel-mt"
version = "0.1.0"
description = "Translation for Eco. Placeholder — no model implemented yet."
requires-python = ">=3.12"
license = "Apache-2.0"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
