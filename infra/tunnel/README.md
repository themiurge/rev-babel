# infra/tunnel

Cloudflare Tunnel configuration for exposing the web app without forwarding
ports on the home router — see
[ADR 0003](../../docs/decisions/0003-cloudflare-tunnel.md).

## Status

Placeholder. No tunnel configuration exists yet. TODO: hostname, tunnel
credentials handling (never committed — see `docs/data-and-privacy.md`),
and routing to `apps/web` are not yet decided.
